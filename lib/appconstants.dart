// lib/approutes.dart
import 'package:flutter/material.dart';

class AppConstants {
  //colores
  static const colorText = Color(0xFFD4D4D4);
  static const colorBackground = Color(0xFF0A0601);
  static const colorBackgroundLighter = Color(0xFF161513);
  static const colorHefesto = Color(0xFFEB441A);
  static const colorHefestoSemitransparente = Color(0xBBEB441A);
  static const colorHefestoBackground = Color(0x1AEB441A);
  static const colorHercules = Color(0xFFE99A1A);
  static const colorHerculesSemitransparente = Color(0xBBE99A1A);
  static const colorHerculesBackground = Color(0x1AE99A1A);
  static const colorHermes = Color(0xFF2857DA);
  static const colorHermesSemitransparente = Color(0xBB2857DA);
  static const colorHermesBackground = Color(0x1A2857DA);

  // Fuente
  static const String fontFamily = 'Roboto';

  // Altura de la BottomNavbar
  static const double bottomNavbarHeight = 20.0;

  // Otros estilos o textos globales que quieras definir
}
